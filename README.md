# Xray-Disease-Classifier
This project explores whether traditional machine learning models (e.g., SVM, logistic regression, KNN, perceptron) can effectively detect pneumonia from chest X-ray images. We used a binary classification task (Normal vs. Pneumonia) and tested how preprocessing methods like scaling and PCA affect performance.
